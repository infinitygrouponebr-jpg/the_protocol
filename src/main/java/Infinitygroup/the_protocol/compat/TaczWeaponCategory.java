package Infinitygroup.the_protocol.compat;

/** Normalized categories returned from TaCZ gun-pack index data. */
public enum TaczWeaponCategory {
    PISTOL, SMG, SHOTGUN, RIFLE, SNIPER, LMG, LAUNCHER, HEAVY, UNKNOWN
}
